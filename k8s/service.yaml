apiVersion: v1
kind: Service
metadata:
  name: presidio-nl-v2
  namespace: presidio-nl-v2
  labels:
    app: presidio-nl-v2
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: presidio-nl-v2 